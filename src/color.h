#pragma once

#include <SFML/Graphics.hpp>

// some forward declarations
namespace Color {
	extern sf::Color dark;
	extern sf::Color light;
	extern sf::Color legalMove;
	extern sf::Color lastestMove;
};
